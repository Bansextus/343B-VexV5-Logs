{"mode":"Text","hardwareTarget":"brain","textContent":"from vex import *\n\n# Brain and Controller\nbrain = Brain()\ncontroller = Controller(PRIMARY)\n\n# ----------------------------\n# LEFT SIDE (3 motors)\n# Flip True/False only if a motor spins wrong\n# ----------------------------\n\nleft_front  = Motor(Ports.PORT7,  GearSetting.RATIO_18_1, False)\nleft_middle = Motor(Ports.PORT20, GearSetting.RATIO_18_1, False)\nleft_back   = Motor(Ports.PORT18, GearSetting.RATIO_18_1, False)\n\n# ----------------------------\n# RIGHT SIDE (3 motors)\n# ----------------------------\n\nright_front  = Motor(Ports.PORT9,  GearSetting.RATIO_18_1, True)\nright_middle = Motor(Ports.PORT14, GearSetting.RATIO_18_1, True)\nright_back   = Motor(Ports.PORT2, GearSetting.RATIO_18_1, True)\n\n# Motor Groups\nleft_drive  = MotorGroup(left_front, left_middle, left_back)\nright_drive = MotorGroup(right_front, right_middle, right_back)\n\n# Outtake\nouttake = Motor(Ports.PORT12, GearSetting.RATIO_18_1, False)\n\n# Brake mode\nleft_drive.set_stopping(BRAKE)\nright_drive.set_stopping(BRAKE)\nouttake.set_stopping(BRAKE)\n\n# ----------------------------\n# Driver Control (Arcade Drive)\n# Axis3 = forward/back\n# Axis1 = turn\n# ----------------------------\n\ndef usercontrol():\n    while True:\n\n        forward = controller.axis3.position()\n        turn = controller.axis1.position()\n\n        # Arcade mix\n        left_speed = forward + turn\n        right_speed = forward - turn\n\n        # Clamp speeds\n        left_speed = max(min(left_speed, 100), -100)\n        right_speed = max(min(right_speed, 100), -100)\n\n        # Spin motors\n        left_drive.spin(FORWARD, left_speed, PERCENT)\n        right_drive.spin(FORWARD, right_speed, PERCENT)\n\n        # Outtake at 100%\n        if controller.buttonR1.pressing():\n            outtake.spin(FORWARD, 100, PERCENT)\n        elif controller.buttonR2.pressing():\n            outtake.spin(REVERSE, 100, PERCENT)\n        else:\n            outtake.stop()\n\n        wait(20, MSEC)\n\n# Competition setup\ncomp = Competition(usercontrol, None)\n","textLanguage":"python","robotConfig":[],"slot":5,"platform":"V5","sdkVersion":"20240802.15.00.00","appVersion":"4.64.0","fileFormat":"2.0.0","targetBrainGen":"First","v5Sounds":[{"name":"game over","url":"static/sounds/mixkit-arcade-retro-game-over-213.wav"}],"v5SoundsEnabled":false,"aiVisionSettings":{"colors":[],"codes":[],"tags":true,"AIObjects":true,"AIObjectModel":[],"aiModelDropDownValue":null}}